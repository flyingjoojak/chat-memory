# 세션 동기화 설계서 (Session Sync SPEC) — Phase 1: Syncthing 기반

> 목적: 한 사용자가 **여러 기기**에서 Claude Code를 쓸 때, 세션(대화 트랜스크립트)을 자동 동기화해
> 어느 기기에서든 이어서 재개(`claude --resume`)·검색할 수 있는 **세션 통일성**을 제공한다.
> (Obsidian Sync / Syncthing 같은 경험, 단 **대화 원문은 제3자에게 노출하지 않는다**는 프로젝트 철학 유지.)

- 상태: **초안 (설계 검토용)**
- 범위: **Phase 1 = Syncthing 전송 + chat-memory 얇은 헬퍼**. Phase 2(네이티브 델타 동기)는 §11에서 미리보기만.
- 관련 문서: 저장소 `SPEC.md`(메인 설계), `README.md`

---

## 1. 목표 / 비목표

### 목표
- 기기 A에서 만든/진행한 세션을 기기 B·C에서 **자동으로 사용 가능**하게 한다.
- B에서 그 세션을 **`claude --resume`으로 이어서** 진행할 수 있다(같은 세션 id에 append).
- 동기된 세션이 각 기기의 **chat-memory 검색/지도에도 반영**되어 검색까지 멀티기기 통일.
- **프라이버시 우선**: 대화 원문이 신뢰할 수 없는 제3자 저장소에 평문으로 남지 않는다.
- **안전한 삭제**: 한 기기의 실수/정리가 다른 기기의 이력을 파괴하지 않는다.

### 비목표 (Phase 1)
- 같은 세션을 **두 기기에서 동시에 실시간 편집**하는 협업(구글독스형 실시간 머지)은 지원하지 않는다. (단일 작성자 가정)
- 클라우드 호스팅형 SaaS 동기 서비스 제공(그건 Phase 2 이후 선택).
- Claude Code 자체 수정(우리는 파일 레이어에서만 동작).

---

## 2. 배경과 핵심 통찰

### 데이터 특성
- 세션 = `~/.claude/projects/<encoded-cwd>/<session-id>.jsonl` **1파일**.
- **append-only 로그**: 대화가 진행되며 줄(JSON) 만 뒤에 추가됨. 과거 줄은 바뀌지 않음.
- 세션 id = UUID, **기기 바인딩 없음**. 최신 Claude Code는 `--resume <id>` 시 프로젝트 폴더를 넘어 **전역 검색**으로 세션을 찾음 → 파일이 `~/.claude/projects/` 아래 아무 폴더에 있어도 재개 가능.
- 폴더명 인코딩: 절대 cwd의 비영숫자 문자를 `-`로 치환(예: `C:\Users\JHJOO` → `C--Users-JHJOO`). 사용자명이 기기마다 달라도 **재개엔 무관**(전역 검색 + cwd는 과거 맥락일 뿐).

### 핵심 통찰 — "append-only + 단일 작성자" ⇒ 충돌 해소가 거의 공짜
- 한 세션은 보통 한 순간 **한 기기에서만** 진행된다.
- B에서 재개하면 B파일 = A파일 + 새 줄 → **B는 A의 상위집합(superset)**.
- 따라서 대부분의 "충돌"은 **superset-wins**(한쪽이 다른쪽의 prefix면 긴 쪽 채택)로 무손실 해소된다.
- **진짜 충돌**(같은 세션을 두 기기에서 동시에 이어 두 갈래로 갈라짐)만 예외 → **fork로 둘 다 보존**하고 사용자에게 표시.

이 통찰이 Syncthing의 유일한 약점(전체파일 충돌 → `.sync-conflict` 사본 생성)을 **간단한 후처리 리졸버**로 메우게 해준다.

---

## 3. 아키텍처 개요

```
┌────────────── 기기 A ──────────────┐        ┌────────────── 기기 B ──────────────┐
│ Claude Code → ~/.claude/projects/  │        │ Claude Code → ~/.claude/projects/  │
│         │  (jsonl append)          │        │         ▲  (jsonl append)          │
│         ▼                          │        │         │                          │
│   [Syncthing 폴더]  ⇄⇄⇄  P2P·TLS(E2E) ⇄⇄⇄  [Syncthing 폴더]                     │
│         │                          │        │         │                          │
│         ▼                          │        │         ▼                          │
│  chatmem sync-helper               │        │  chatmem sync-helper               │
│   - superset 충돌 리졸버           │        │   - superset 충돌 리졸버           │
│   - 활성 세션 가드                 │        │   - 활성 세션 가드                 │
│   - 동기 후 자동 색인(커서 재사용) │        │   - 동기 후 자동 색인              │
└────────────────────────────────────┘        └────────────────────────────────────┘
```

- **전송 계층 = Syncthing**: 검증된 오픈소스 P2P 파일 동기. 기기 간 **TLS 종단암호화**, 인증서 기반 device ID, LAN 직접연결 + (선택) 암호화 릴레이. 제3자 저장소 없음.
- **chat-memory sync-helper**(우리 코드): Syncthing이 못 하는 **도메인 특화 처리**만 담당.
  1. `.sync-conflict` 파일을 **superset-wins**로 자동 정리(또는 fork 보존).
  2. **활성 세션 가드**(진행 중 세션의 조기 동기/이중 재개 방지 신호).
  3. 동기로 새로 들어온 jsonl을 **chat-memory 증분 색인**(기존 커서/오프셋 재사용).

> 설계 원칙: **전송은 검증된 도구에 위임, 우리는 얇게**. Phase 1은 "Syncthing + 리졸버 스크립트/데몬" 수준으로 최소화.

---

## 4. 신뢰 / 프라이버시 모델 (미결 — 여기서 검토)

Syncthing은 기기 간 전송을 항상 E2E 암호화하지만, **"기기 발견(discovery)"과 "연결 중계(relay)"를 어디에 의존하느냐**로 신뢰 모델이 갈린다.

| 모델 | 발견/중계 | 원문 노출 | 편의성 | 비고 |
|---|---|---|---|---|
| **M1. LAN 전용** | 로컬 발견만 | 없음(같은 네트워크만) | ★ (같은 망 필요) | 가장 폐쇄적. 외부망 기기 동기 불가 |
| **M2. P2P + 공용 릴레이/발견** | Syncthing 공용 인프라 | **없음**(릴레이는 암호문만 중계, 복호 불가) | ★★★ | 기본값. 원문은 못 봐도 "메타데이터(기기 id·용량·연결시각)"는 공용 서버가 앎 |
| **M3. 자가호스팅 발견+릴레이** | 내 서버/NAS가 발견·릴레이 | 없음 + 메타데이터도 자기 통제 | ★★ | 완전 통제. 서버 하나 운영 |

**추천(Phase 1)**: 기본은 **M2**(편의성↑, 원문 비노출), 프라이버시 민감 사용자를 위해 **M3 옵션** 문서화. M1은 "동일 LAN만 쓰는" 특수 케이스로 안내.

- 원문 기밀은 **어느 모델이든 유지**된다(Syncthing E2E). 차이는 **메타데이터를 누가 보느냐**뿐.
- 별도 파일 암호화(age/git-crypt류)는 Phase 1에선 **불필요**(P2P라 저장소에 평문이 안 쌓임). 클라우드 스토리지로 가는 Phase 2/D안에서만 클라이언트 암호화가 필수.

> **결정 필요(Open Q1)**: 기본 모델을 M2로 하고 M3를 opt-in 문서화하는 데 동의? 아니면 처음부터 M3(자가호스팅) 강제?

---

## 5. 컴포넌트 상세

### 5.1 동기 대상 폴더 구성
- 동기 루트 후보:
  - (A) `~/.claude/projects/` **전체** — 단순. 모든 프로젝트/세션 통일.
  - (B) **선택 서브셋** — 특정 프로젝트만. `.stignore`로 제외.
- **권장**: 기본은 (A) 전체, `.stignore`로 예외 관리(§5.5).
- `~/.claude/` 하위의 **설정/자격증명/토큰 등은 동기 제외**(projects만). 세션 외 파일이 새 나가지 않게 루트를 `projects/`로 한정.

### 5.2 활성 세션 가드
문제: A에서 **진행 중**인 세션을 B가 당겨 **동시에 재개**하면 진짜 fork 발생.
- **파일 무결성**은 Syncthing이 **원자적 교체(temp→rename)**로 써서 안전 — 리더가 반쪽 파일을 볼 일은 없음(찢긴 마지막 줄 리스크 낮음).
- 남는 위험은 **동시 재개(이중 작성자)**. 대응:
  1. `chatmem` 재개("열기") 시 그 세션이 **다른 기기에서 활성**이면 경고/차단. 활성 신호는 작은 사이드카(예: `<id>.lock` 또는 동기되는 인덱스의 `active_on`/`heartbeat`)로 전파.
  2. 활성 판정: 해당 jsonl의 최근 mtime(N분 내) + 로컬 `claude` 프로세스 존재.
- Phase 1 최소안: **경고만**(하드 차단은 옵션). 진짜 fork가 나도 §5.3이 둘 다 보존.

### 5.3 충돌 해소 (superset-wins + fork)
Syncthing이 충돌 시 `~/.claude/projects/<...>/<id>.sync-conflict-<날짜>-<시각>-<기기>.jsonl` 사본을 만든다. sync-helper가 감지해서 정리:

```
resolve(base = <id>.jsonl, conflict = <id>.sync-conflict-*.jsonl):
  a, b = read_lines(base), read_lines(conflict)
  if is_prefix(a, b):        # base ⊂ conflict  → conflict가 최신 상위집합
      base ← conflict; rm conflict            # superset-wins
  elif is_prefix(b, a):      # conflict ⊂ base
      rm conflict                             # base 유지
  else:                      # 진짜 분기(fork)
      new_id = uuid()                         # 분기본을 새 세션으로 보존
      mv conflict → <new_id>.jsonl (+ 인덱스에 'forked_from=<id>' 기록)
      사용자에 표시("이 세션이 두 기기에서 갈라짐 → 새 세션으로 보존")
```
- 비교는 **줄 단위 prefix** 검사(값 재해석 없이 바이트/라인 비교). append-only라 대부분 prefix 케이스.
- fork는 데이터 무손실이 원칙(어느 쪽도 안 지움).

### 5.4 삭제 안전
- Syncthing 폴더를 **파일 버저닝(휴지통/staggered)** 켜서 삭제·덮어쓰기 이력 보존.
- 필요 시 기기별 폴더 타입 조정: 아카이브 기기는 **receive-only**로 두어 그 기기에서의 실수 삭제가 역전파되지 않게.
- 정책: **자동 영구삭제 금지**. 삭제는 툼스톤/버전으로만.

### 5.5 선택 동기 (`.stignore`)
- 기기 전용/민감 프로젝트 제외. 예:
```
// ~/.claude/projects/.stignore
(?d)*.sync-conflict-*        // 리졸버가 처리하므로 원격전파 최소화(선택)
<encoded-cwd-of-local-only-project>/
*.tmp
```
- **주의**: 활성 세션을 `.stignore`로 동적으로 넣다 빼는 방식은 복잡 → Phase 1은 §5.2 신호 방식 우선.

### 5.6 (선택) 경량 인덱스 & lazy pull
- 작은 `sync-index.json`(세션 id → 프로젝트, 최종수정, 출처기기, active heartbeat)만 상시 동기.
- UI에서 "다른 기기 세션 목록"을 보여주고 **필요한 것만 당겨오기**(대역폭·저장 절약). Phase 1은 optional, Phase 2에서 본격화.

---

## 6. chat-memory 통합
- **설정 탭 / CLI에 "세션 동기" 섹션** 추가:
  - Syncthing 설치·기기 페어링 **가이드**(원클릭까진 아님, 안내+상태 배지).
  - sync-helper 데몬 on/off, 충돌 리졸버 로그, 선택 동기 글롭 편집.
- **동기 후 자동 색인**: 새/변경 jsonl 감지 → 기존 **커서/오프셋 증분 리더**로 append분만 색인. (이미 있는 파이프라인 재사용, 추가 임베딩 비용 최소.)
- 재개("열기") 버튼과 연동: §5.2 활성 가드 경고를 여기서 노출.

---

## 7. 엣지케이스 표

| 상황 | 처리 |
|---|---|
| B가 A의 최신 세션을 받고 재개(순차) | B파일=A의 superset → 정상, 같은 id에 append |
| A 진행 중 B가 동시 재개 | fork 발생 → §5.3이 분기본을 새 세션으로 보존 + 경고 |
| 사용자명/경로 다른 기기 | 폴더명만 다름, 전역 검색으로 재개 무관(§2) |
| 마지막 줄 찢김 | Syncthing 원자적 교체로 사실상 없음; 리더는 완결 스냅샷만 봄 |
| 한 기기서 세션 삭제 | 버저닝/receive-only로 이력 보존, 자동 영구삭제 금지 |
| 오프라인 후 재연결 | Syncthing이 자동 재동기; 리졸버가 밀린 충돌 정리 |
| 대용량 세션 다수 | 전체파일 동기라 초기 부하↑ → 선택 동기/lazy(§5.6)로 완화, Phase 2 델타로 근본 해결 |

---

## 8. 보안 고려
- 전송: Syncthing **TLS E2E**, device ID = 인증서 지문(중간자 불가).
- 저장: P2P라 **제3자 평문 저장 없음**(§4). 릴레이는 암호문만 중계.
- 노출면: 동기 폴더를 **`projects/`로 한정**해 자격증명/설정 유출 차단.
- 기기 분실 대비: 기기 폐기 시 device 링크 해지. (선택) 민감 사용자용 M3 자가호스팅.
- chat-memory DB/벡터는 **동기 대상 아님**(각 기기 로컬 재색인). 원문은 jsonl로만 이동.

---

## 9. 설정 예시(참고)
- Syncthing 폴더: `~/.claude/projects` 를 공유 폴더로 추가, 두 기기에서 동일 Folder ID로 공유.
- 파일 버저닝: "Staggered File Versioning" 켜기(삭제·덮어쓰기 이력).
- 아카이브 기기: Folder Type = **Receive Only**.
- `.stignore`: §5.5.
- sync-helper: 폴더 감시(watchdog) → `.sync-conflict-*` 등장 시 §5.3 실행 + 동기분 증분 색인.

---

## 10. 구현 계획(마일스톤)
1. **M1 — 리졸버 코어**: `.sync-conflict` superset-wins/fork 리졸버(순수 함수 + 테스트). *(전송 무관, 단독 테스트 가능)*
2. **M2 — 감시 데몬**: `projects/` 워처 → 충돌 리졸브 + 동기분 증분 색인 트리거.
3. **M3 — 활성 가드**: 활성 판정 + 재개 경고 연동(사이드카/heartbeat).
4. **M4 — 설정 UI/가이드**: Syncthing 페어링 안내, 데몬 토글, 선택 동기 편집, 상태 배지.
5. **M5 — 문서**: 사용자 셋업 가이드(M2/M3 신뢰모델 선택 포함).

> M1은 전송(Syncthing) 없이도 단위 테스트 가능 → **여기부터 PoC 시작 권장**.

---

## 11. Phase 2 미리보기 — 네이티브 append-델타 동기(선택)
- chat-memory 내장 클라이언트가 **커서/오프셋으로 추가 바이트만** 전송(전체파일 X).
- 백엔드: 자가호스팅 서버 또는 오브젝트 스토리지(R2/S3) + **클라이언트 E2E 암호화**.
- lazy pull(경량 인덱스로 목록만, 본문은 on-demand), 충돌정책 내장, "다른 기기 세션" 탭.
- 이점: Syncthing 설치 불필요(배포형 완성), 초기 대용량 부하 없음.
- 대가: 서버/스토리지 운영 + 암호화·인증·충돌 구현. → **Phase 1 안정화 후 착수**.

---

## 12. 오픈 퀘스천 (결정 필요)
- **Q1(신뢰모델)**: 기본 M2(공용 발견/릴레이, 원문 비노출) + M3 opt-in 문서화로 OK? vs 처음부터 M3 강제?
- **Q2(동기 범위)**: `~/.claude/projects/` 전체 기본 + `.stignore` 예외 vs 선택 프로젝트만 opt-in?
- **Q3(활성 충돌)**: 이중 재개를 **경고만** vs **하드 차단**?
- **Q4(삭제)**: 버저닝 + receive-only 아카이브 기기 권장안 채택?
- **Q5(범위)**: Phase 1을 "리졸버+데몬+가이드"까지로 볼지, 설정 UI(M4)까지 포함할지?

---

## 13. 로드맵 & 알려진 특성 / 한계

설계상 나온 특성·제약과 그에 대응하는 향후 방향 정리. (Phase 1은 그대로 두고, 아래는 대부분 Phase 2+ 선택지)

### 13.1 가용성 — 순수 P2P는 "동시 온라인" 필요
- **특성**: Syncthing 2대만이면 A·B가 **동시에 온라인일 때만** 실제 전송. 한쪽이 꺼져 있으면 변경분은 로컬에 쌓였다가 **다음에 겹칠 때 몰아서** 동기 → **손실은 없고 지연만**. 겹치는 시간이 아예 없으면 영영 안 맞음.
- **참고**: Syncthing 공용 릴레이는 NAT 우회용 **실시간 중계**지 **오프라인 보관소가 아님**(이것도 둘 다 온라인 필요).
- **대응**: **항상 켜진 3번째 노드(허브)**를 두면 비동기화됨 — A는 A 켜졌을 때, B는 B 켜졌을 때 각각 허브와 맞춤 → 동시 온라인 불필요.
  - 허브 후보: 집 NAS / 라즈베리파이 / 상시 데스크탑 / 저가 VPS.
  - 클라우드 VPS 허브라도 Syncthing **"Untrusted(암호화) 기기"** 기능으로 **암호문만 저장** → 원문 비노출 유지. (신뢰모델 §4의 M3와 연결)

| 구성 | 동시 온라인 필요 | 프라이버시 | 비용/수고 |
|---|---|---|---|
| 2대만(순수 P2P) | 필요 | ★★★ | 0 |
| + 상시 허브(NAS/파이) | 불필요 | ★★★(내 통제) | 하드웨어 1 |
| + 클라우드 VPS 허브(암호화) | 불필요 | ★★★(암호문만) | 월 소액 |
| Phase 2 자체 서버 | 불필요 | ★★★(E2E) | 구현+운영 |

> **오픈 Q6**: 상시 허브를 권장 구성으로 문서화할지, Phase 2 서버로 갈지.

### 13.2 기기별 3D 지도 상이 (파생물 로컬 생성의 부수효과)
- **특성**: 3D 지도는 `로컬 벡터 → UMAP → HDBSCAN → 좌표/군집/라벨`로 만들어진다. 입력이 **그 기기의 임베딩**이라:
  - 점 **좌표·배치**, **군집 구성/개수** = 기기별로 다를 수 있음(모델 다르면 벡터공간 다름).
  - 군집 **라벨**·세션 **선(성좌)**·**어떤 대화가 있는지**는 대체로/완전히 동일(원본·태그 기반).
  - 같은 모델이어도 UMAP의 **확률적 요소** + **기기별 캐시(`graph3d_cache.json`)** 때문에 스냅샷이 미세하게 어긋날 수 있음.
- **영향**: 지도는 "탐색용 시각화"라 실사용 무해. **무엇이 있는지는 같고 배치·군집만 다름.**
- **대응(원하면)**: 기기 간 지도까지 통일하려면 **① 임베딩 모델 통일 + ② 벡터/좌표·`graph3d_cache.json` 동기**. 단 "각자 임베딩" 설계를 벗어남(Phase 2 성격).

### 13.3 기기별 임베딩 모델 자유 (저사양 대응)
- **특성**: 벡터를 기기 간 안 나르므로 **기기마다 다른 임베딩 모델** 사용 가능. 저사양 기기 → 가벼운 모델(작은 벡터 = RAM·디스크도 절약).
- **제약**: 한 기기 **안에서는** 저장 벡터와 질의 벡터가 **동일 모델**이어야 함(공간·차원 일치). 모델 교체 시 그 기기 인덱스 **전체 재임베딩** 필요.
- **트레이드오프**: 열람·재개·키워드(FTS5) 검색은 **어디서나 동일**. 차이는 **의미검색 정밀도**뿐(작은 모델 = 약간 낮음).
- **주의**: 향후 "임베딩 결과 공유"(중복 임베딩 제거)를 하려면 **전 기기 모델 통일** 필요 → **"기기별 모델 자유" vs "임베딩 공유"는 양립 어려움**.

### 13.4 계정 기반 자동 기기 등록 (제어 평면) — Phase 3 후보
- **아이디어**: 프로그램에 계정을 두고, **로그인만으로 그 계정의 기기를 불러와 동기 설정을 자동 구성**. (Obsidian Sync식)
- **3평면 모델**:
  - **제어 평면(계정)**: 기기 명단·페어링·폴더설정·(암호화된) 키 배포 — *새로 추가*
  - **데이터 평면**: 세션 jsonl 원본(E2E, 기기↔기기 or 암호화 허브)
  - **로컬 평면**: 기기별 임베딩·DB·3D지도
- **등록 흐름**: `로그인 → 기기 키페어 생성 → device-id/공개키 계정 등록 → 서버가 기기 명단 반환 → 앱이 Syncthing 설정 API로 mesh 자동 구성(피어·폴더) → 완료`. (Syncthing REST 설정 API로 프로그램적 구성 가능)
- **프라이버시 불변식(엄수)**: **계정 서버는 "누가·어떤 기기"만 알고 "세션 내용은 절대 모름".** 원문·평문키는 제어 평면에 없음.
  - 서버 보유: 계정, 기기 id·공개키, 폴더 멤버십, (선택) **wrapped key**.
  - 마스터 키는 **사용자 패스프레이즈 파생**(서버 미전송). 새 기기 로그인 시 패스프레이즈로 키 해제 → 서버는 암호문만.
- **이점/대가**: 로그인 한 번에 새 기기 자동 세팅·중앙 관리(원격 링크 해지) ↔ 인증 서버 운영·계정 보안(새 공격면)·키 복구/에스크로 설계. 잘못하면 "원문 안 올림" 철학 훼손 위험 → **경계 엄수**.
- **위치**: Phase 2에서 서버/스토리지가 생긴 뒤 그 위에 얹는 순서. Phase 1(수동 페어링)엔 불필요.

> **오픈 Q7**: 계정 제어 평면을 로드맵에 정식 포함할지(Phase 3), 키 에스크로(비번 분실 복구) 지원 여부.
