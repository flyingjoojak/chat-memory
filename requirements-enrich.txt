# 정제(enrich) 백엔드가 "claude"(기본, claude -p 구독)나 "off"면 아무것도 필요 없음.
# 아래는 백엔드별 선택적 의존성 — 쓰는 것만 설치하면 된다.
#
#   anthropic 백엔드           → anthropic
#   openai / gemini / ollama   → openai   (셋 다 OpenAI 호환 API라 SDK 하나로 처리)
#
# 설치 예: pip install openai        (GPT / Gemini / Ollama 로컬)
#          pip install anthropic     (Anthropic API)
anthropic>=0.40
openai>=1.40
