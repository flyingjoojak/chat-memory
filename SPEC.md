# chat-memory — 대화 정보자산 축적·검색 시스템 SPEC

> 상태: **전 항목·세부 전부 확정 — 구축 착수 대기** · 최종수정 2026-07-24
> 확정: 4.1 원문verbatim+2단자산화 / 4.2 지연배치+JSONL커서·다중세션 / 4.3 e5-large / 4.4 독립벡터DB / 4.5 CLI+코어라이브러리 / 4.6 전체포착 / 4.7 작업턴 균형캡처 / 4.8 맥락 3중전략 / 4.9 정제=claude -p·임베딩=로컬fastembed.
> 목표: Claude Code 대화를 자동 누적하여 **검색 가능한 개인 정보자산**으로 축적한다.

---

## 1. 목적 (Why)

AI와 나눈 질문·답변·판단·해법이 대화창을 닫으면 사라진다.
이를 **자동으로 누적 저장 + 벡터 검색**하여, "그때 그거 어떻게 했지?"를
언제든 되찾을 수 있는 **개인 지식자산 층**을 만든다.

기존 자산 층과의 관계:
- `MEMORY.md` (자동 메모리) — 압축된 사실/선호
- Obsidian 볼트 (개인 노트) — 수기 지식
- **chat-memory (신규)** — AI 대화 원본에서 추출한 검색가능 지식 ← 3번째 층

---

## 2. 실현가능성 (검증 완료 · 전부 GREEN)

| 항목 | 결과 | 근거 |
|---|---|---|
| 대화 자동수집 | ✅ | Claude Code가 JSONL 자동저장. 현재 270세션 / 190MB 존재 |
| Q&A 파싱 | ✅ | 메시지 구조 확인: user=Q, assistant.text=A. thinking/tool/명령어/isMeta 필터링 |
| 로컬 임베딩 | ✅ | fastembed 0.8.0 + numpy 2.2.6 설치 통과 (Win/Py3.10) |
| 한국어 모델 | ✅ | MiniLM(384)/mpnet(768)/e5-large(1024) 로컬 사용 가능 |
| 검색 | ✅ | numpy 코사인 브루트포스 (수천~수만 청크면 즉시). DB 불필요 |
| 미래 대화 실시간(턴 단위) | ✅ | Stop 훅=매 턴 종료 시 실행 / UserPromptSubmit 훅=질문 즉시 |

---

## 3. 아키텍처

```
JSONL 로그 (Claude Code 자동생성)
   │  ① 파서: user/assistant 턴 추출 → Q&A
   ▼
[선택 ②] 정제: 요약·태깅              ← 자산의 "결" 결정 (아래 4.1)
   ▼
③ 임베딩 (로컬 ONNX, 고정 모델 1종)
   ▼
④ 벡터 인덱스 (numpy .npy + 메타 .jsonl, 누적)
   ▼
⑤ 검색 CLI  (+ 선택: Obsidian md 내보내기)
```

### 실시간 자동화 (미래 대화)
- `UserPromptSubmit` 훅 → 질문 캡처
- `Stop` 훅 (매 턴 종료) → 답변 캡처 + 그 턴만 증분 임베딩 → 인덱스 append
- 실행 방식은 A/B 중 택1 (아래 4.2)

### 과거 백필
- 270개 세션 1회 배치 파싱 → 임베딩 → 초기 인덱스 구축 (수십 분 예상, 최초 1회)

---

## 4. 미결 결정사항 (논의 대상)

### 4.1 자산의 "결" — ✅ 확정: 원문 verbatim + 2단 자산화
- **원문 Q&A를 그대로 저장**하고 임베딩·검색결과로 원문을 반환 → 맥락 손실 0.
- 요약/태그는 원문을 대체하지 않고 **부가 메타로 덧붙임**(정제형=원본형의 상위집합).
- **2단 분리**:
  - 근실시간 배치: JSONL 델타 → 원문 청킹 → 임베딩 → 인덱스 추가 (LLM 안 씀, 검색 반영 수 분 이내)
  - 야간 배치(자동 스케줄): Claude로 요약·태깅·중복병합해 자산 숙성 (원문 불변)

### 4.2 실행 방식 — ✅ 확정: 지연 배치 (데몬 없음)
> ※ 임베딩 모델만 실행. Claude(LLM) 병렬실행 아님. 대화 지연·검색품질과 무관.
- **데이터 출처 = Claude Code JSONL 로그** (앱이 모든 턴을 빠짐없이 기록하는 진실원본). 큐/훅 가로채기 아님.
- 트리거: 타이머(수 분마다) 또는 Stop 훅. 트리거가 실패해도 다음 주기가 JSONL을 다시 훑어 회수.
- 처리: 세션별 **오프셋 커서**로 새 줄만 증분 처리 → 모델 로드 → 밀린 것 임베딩 → 종료.
- **상주 RAM 평소 0GB** (배치 도는 몇 초만 ~2.2GB 점유 후 반납). 데몬·자동시작 등록 불필요.
- **누락 방지 보장**: 커서는 저장 성공 후에만 전진(at-least-once) + `(sessionId+turnUUID)` 멱등 키(중복 방지).
  대화 속도와 무관하게 손실 0. 크래시/트리거실패/폭주 모두 다음 주기가 회수.

- **다중 세션**: 터미널(세션)당 JSONL 1개. 배치가 `~/.claude/projects/**/*.jsonl` 전부를 세션별 커서로 병렬 처리. 세션끼리 같은 파일 안 씀 → 충돌 없음. 쓰이는 중 파일은 마지막 완결 줄까지만 읽음(tail-safe).

### (참고) 초 단위 신선도가 필요해지면 — 자동수면 데몬으로 승격
- 대화 중만 모델 상주(2.2GB), N분 유휴 시 자동 언로드 → 유휴 RAM 0. 코드 일부 추가.

### 4.3 임베딩 모델 — ✅ 확정: multilingual-e5-large (1024d), 백엔드 교체가능
> 색인·검색 공통, 변경 시 전체 재색인.
- 품질 최우선 + 백그라운드라 속도 무관 → 경량 모델 쓸 이유 없음.
- 검색정확도·차원·한↔영 교차·컨텍스트 모두 3후보 중 최상.
- 컨텍스트 윈도우 ~512토큰 → 긴 턴은 **overlap 청킹**으로 커버(맥락 보존은 청킹+원문저장이 담당).
- **임베딩 백엔드는 교체 가능하게 설계**: 기본 로컬 `fastembed`. 나중에 로컬 제약을 완화하면 **Voyage AI(코드·다국어 강함, 단 원문 외부전송·과금)** 등으로 스위치 전환 후 재색인 가능(원문 보관하므로 무손실).

### 4.7 작업 턴(코드·파일수정·명령) 캡처 범위 — ✅ 확정: (b) 균형
- JSONL엔 작업 전체가 담김: `Write`(file_path+content), `Edit`(old/new diff), `Bash`(command), tool_result(출력).
- **아카이브(손실 0)**: 턴 전체 + 행동 로그(Read/Edit/Wrote/Ran + 코드·diff 원문) 보존.
- **임베딩(검색)**: 질문 + 설명텍스트 + 행동요약(건드린 파일·명령·결정)만. **거대 raw tool_result는 아카이브만, 임베딩 제외**(검색 노이즈 방지).
- (반려) (a)최종답변만=작업유실 / (c)전부임베딩=인덱스 비대·노이즈.

### 4.8 이어지는 맥락 보존 — ✅ 확정: 3중 전략
- **(A) 스레드 링크**: 턴 레코드에 `sessionId+parentUuid` 저장 → 검색 히트 시 앞뒤 턴/스레드 통째 반환.
- **(B) 맥락 임베딩 (Contextual Retrieval)**: 턴을 고립 임베딩하지 않고 앞 맥락을 덧대 임베딩.
  - 실시간: 직전 턴 N개 prepend(비용 0). 야간: Claude로 정교한 맥락 blurb 생성 후 재임베딩.
- **(C) 이중 입도**: 턴 단위 청크 + 세션/주제 단위 요약 레코드 → 두 층위 검색(특정 턴 / 세션 전체 맥락).

### 4.9 역할 분담 — ✅ 확정
- **정제(요약·태그·맥락 blurb) = `claude -p` 헤드리스 (옵션 A)**. 스케줄러가 필요시 호출·종료(터미널 상주 불필요). 구독 토큰 사용.
- **임베딩(텍스트→벡터) = 로컬 fastembed 고정**. LLM/서브에이전트 위임 **불가**(LLM은 벡터를 생성하지 못함; Anthropic 임베딩 API 없음). 별도 프로세스라 대화 영향 0.
- 유일 제약: 배치 시 PC 전원 ON. 스케줄러 "놓친 작업 재실행"으로 보완.

### 4.4 저장 위치 — ✅ 확정: 독립 벡터DB만
- chat-memory 폴더에 인덱스(.npy) + 메타(.jsonl) + 원문 아카이브. Obsidian 내보내기는 **함수로 나중 추가**.

### 4.5 검색 인터페이스 — ✅ 확정: CLI 먼저, 코어 라이브러리 분리
- 지금: `mem "검색어"` CLI. 로직은 전부 **코어 라이브러리**(index/search/reindex/export)에 둠.
- 나중: 자체 로컬 앱(추천 FastAPI+React)이 같은 코어를 재사용 → UI만 얹음. 재작업 최소.
- **앱에서 임베딩 모델 변경**: 가능. 단 전체 재색인 필요(벡터 비호환). **원문 아카이브 보관 → 무손실 재임베딩.** 인덱스에 모델명 기록·검색 시 일치확인.
- **Obsidian 내보내기**: 아카이브 순회 → 볼트에 md 생성. export 함수로 제공.

### 4.6 프라이버시 범위 — ✅ 확정: 전체 포착
- 모든 프로젝트의 모든 세션 포착. 전부 로컬이라 외부유출 0. (추후 제외목록 옵션 여지 남김)

### ★ 설계 원칙 (전 기능의 근간)
> **원문 아카이브 = 진실원본 / 벡터 인덱스 = 재생성 가능한 파생물.**
> 원문을 항상 보관하므로 모델 변경·재색인·내보내기·앱 확장이 전부 무손실로 가능.

---

## 5. 구현 세부 결정 (기본값 제안 — 확인/조정 대상)

### 내가 기본값으로 정해도 되는 것 (승인 시 이대로)
- **청킹(격상 확정)**: 턴 기반 — 턴 ≤512토큰이면 1턴=1청크(안 쪼갬). 긴 턴만 recursive 경계분할(문단·코드블록 존중, ≤512, overlap 50) + **부모-자식 반환**(서브청크 매칭→턴 전체 반환). 주류 baseline(recursive+overlap)+검증된 업그레이드(parent-child, contextual) 조합. 의미기반은 제외.
- **아카이브 형식(확정)**: **SQLite**(stdlib, 무의존성). 턴 레코드 = id(sessionId+turnUUID)·session_id·parent_uuid·ts·project·question·answer·actions·raw_ref. 벡터는 `.npy`로 분리(하이브리드). 커서 테이블도 SQLite에.
- **턴 필터(확정) 2단계**:
  - 1단계 구조제거(완전삭제): `isMeta`/`system`/`mode`/`file-history-snapshot`/`<command-*>`/local-command 배관 + **`isSidechain:true`(서브에이전트 내부대화) 제외** — 본대화(당신↔Claude)만 포착.
  - 2단계 가치필터(아카이브O·임베딩X): 규칙기반, 판정대상=Q+A+행동요약. **행동 있으면 무조건 임베딩**(작업턴 보호). 행동 없고 유효길이<~15자거나 사소어("ㅇㅇ"·"고마워"·"ok"…)면 임베딩만 스킵(스레드엔 parent_uuid로 잔류). 보수적 편향(애매하면 임베딩).
- **인덱스 엔진(확정)**: 지금은 **numpy 브루트포스**(정확·무의존성·현 규모 최적). 나중 자체앱 **설정 화면에서 현재 벡터 수 기반으로 엔진 추천 표시**(예: 수십만 넘으면 "hnswlib 권장") + **사용자가 원하는대로 수동 선택 가능**. 엔진 교체는 아카이브에서 재색인이라 무손실.
- **백필 순서**: 최근 세션부터 → 과거로(빨리 유용해짐). 초대형 파일(100MB)도 커서로 스트리밍 처리.

### 취향·비용 결정 — ✅ 전부 확정
- **A. 배치 주기 = 10분마다** (PC 켜진 동안, 놓친 작업 재실행). 검색 반영 최대 10분 지연.
- **B. 정제 = Sonnet(`claude -p`), 백필도 정제.** 시점 = 새벽 유휴. 정제는 **세션 단위 1회 호출**(세션 전문 넣어 요약+각 청크 맥락줄 동시 생성) → 호출수 ≈ 세션수(~270), 초대형 세션만 윈도우 분할.
  - **실행 = 구독 기본**: `claude -p`는 Claude Code 인증(구독) 사용 → **토큰당 $ 없음**. 대신 **사용량 쿼터+레이트리밋 소모**. API 키 방식은 선택(그 경우 1회성 ~수십 달러).
  - **완화**: 새벽 실행 + throttle + 며칠 분산 → 레이트리밋·평소작업 방해 방지. 일상(하루 몇 세션)은 쿼터에 무시가능.
  - **백필 2패스**: 패스1 임베딩(로컬·무료·즉시 검색가능) → 패스2 Sonnet 정제+맥락 재임베딩(선택·지연가능). 임베딩 무료라 재임베딩 부담 0.
- **C. 시크릿 = 그대로 저장**(전부 로컬, 유출 0). 나중 마스킹 옵션 여지.
- **D. 검색 CLI = top-5 + 스레드 맥락 표시 + `--session`/`--since` 필터.**
  - **반환물 = 원문(verbatim) + 정제본(요약·태그) 둘 다 나란히 표시.** 정제본은 원문을 대체하지 않는 부가표시(손실 0). → **정제(B) 유지 확정**(제거 안 함).

### 개선 사항 — ✅ 채택
- **검색 품질 eval 셋**: 테스트 질의 10~20개 + 기대 히트 저장 → 모델·청킹·백엔드 변경 시 품질 회귀 객관 측정(상시 벤치).
- **근접중복 다양화**: 같은 주제 반복 질문으로 유사 청크가 몰릴 때 결과에서 중복 억제(diversify) → 체감 품질↑.
- **배치 실행 로그**: 매 배치가 "언제/몇 턴 인덱싱/스킵/에러" 한 줄 기록 → 정상동작 신뢰 + 문제 조기발견.

### 구축 전 검증할 기술 리스크 (결정 아님, 착수 초반 실측)
- ⚠️ **`claude -p`가 작업 스케줄러(비대화 세션)에서 구독 인증으로 동작하는지** — 정제 자동화의 핵심 의존점. 안 되면 setup-token/API 키 대안.
- **e5 프리픽스**: 쿼리 `query:` / 문서 `passage:` 프리픽스 필수(성능 정확성). 구현 시 반드시 처리.

## 6. 예상 규모
- 코어 라이브러리(파서·청킹·임베딩·인덱스·검색) ~300줄 · CLI ~50줄 · 훅/스케줄러 설정
- 의존성: fastembed, numpy (이미 설치). 정제용 claude CLI(이미 있음).
- 로컬 임베딩·검색(과금 회피 목적). 프라이버시는 하드 제약 아님 — 8장 배포 시 중앙 코어/서버로 확장 가능.

## 7. 로드맵 — ✅ CLI(코어) 먼저, 프로그램 나중

> 원칙: 엔진 먼저 만들어 매일 쓰고, 실사용 경험이 프로그램 사양을 정하게 한다(YAGNI). 코어 분리라 프로그램은 나중에 얹어도 재작업 ≈ 0. 원본 JSONL이 보존되므로 백필은 언제든 가능(유실 압박 없음).

### Phase 1 (지금) — 코어 + CLI + 캡처/백필
1. 코어 라이브러리: 파서(JSONL 커서 증분·턴 그룹핑·필터) → 청킹 → 임베딩(e5, query/passage 프리픽스) → SQLite+npy 저장 → 검색.
2. CLI: `mem "질의"` (top-5 + 원문+정제본 + 스레드 + `--session`/`--since`).
3. 백필: 최근→과거, 패스1 임베딩(무료·즉시 검색가능).
4. 리스크 실측: `claude -p` 스케줄러 인증, e5 프리픽스, 증분 캡처 정확성.
5. 자동화: 10분 배치(스케줄러) + 야간 정제(패스2, Sonnet) + 실행로그.

### Phase 2 (나중, 실사용 요구 반영) — 프로그램
- FastAPI+React 로컬앱: 검색 UI, 브라우징·태그필터, 엔진/모델 변경(벡터수 기반 추천+수동), Obsidian 내보내기, eval 셋 대시보드.
- 코어 라이브러리 그대로 재사용.

## 8. 배포 로드맵 (다른 사람도 쓰게 + 멀티기기)

> 트리거는 프라이버시가 아니라 **(a) 벡터 ~100만 돌파 또는 (b) 여러 기기 통합 필요** (먼저 오는 것). 대화는 이미 Anthropic으로 가므로 "완전 로컬"은 하드 제약이 아니며, 로컬 임베딩은 순전히 비용(과금 회피) 선택.

### 8.1 벡터 저장 백엔드 — ✅ 배포 결정: sqlite-vec int8 (pluggable)
실측(4,206벡터·1024d·쿼리200 평균):

| 방식 | 쿼리당 | 크기 | RAM | 정확도(npy기준) | 100만 환산 |
|------|-------|------|-----|----------------|-----------|
| npy fp32 (현재) | 0.41ms | 17MB | **전량 RAM** | 기준 | 4.10GB RAM |
| sqlite-vec fp32 | 18ms | 21MB(디스크) | 극소 | recall@10 97.6% | 5.02GB 디스크 |
| **sqlite-vec int8** | 6.2ms | **5.4MB(디스크)** | 극소 | recall@10 91.7% · top1 95.5% | **1.28GB 디스크** |

- 결정 근거 = 속도(오히려 npy가 15~40배 빠름) 아님 → **저RAM 유저 지원 + 무한 성장 대비 + 원문·FTS·벡터 파일 하나로 통합**. 6ms는 사람 검색에 무의미.
- int8 손실(≈8% recall@10)은 하이브리드(FTS 융합)+정제표시로 체감 미미. 필요 시 fp32 원본 보관→int8 거친검색 후 **재랭킹**으로 회복.
- **백엔드 pluggable**: `개인 1대=npy`(더 빠름) / `배포·저RAM=sqlite-vec`. 이미 `VectorIndex` 추상화돼 있어 구현체 추가만.

### 8.2 배포 아키텍처 — 얇은 클라이언트 + 중앙 코어
- **클라이언트(각 기기)**: 로그 감시 + 커서 + 원문 턴 텍스트 전송. **모델·벡터·DB 없음**(수 MB). 오프라인 시 커서로 재개.
- **중앙 코어(1곳)**: 임베딩·저장·FTS·검색·정제. 모델은 여기만. 검색 질의도 서버가 임베딩.
- 멀티기기 통합은 turn_id(`sessionId:turnUUID` 전역유니크)로 **충돌·중복 없이** 저절로. `device` 태그로 필터.
- 추가 필요: `/ingest` 엔드포인트, 얇은 캡처 클라, 토큰 인증, (규모 시) pgvector.

### 8.3 배포 형태 — ⬜ 미확정 (아키텍처는 셋 다 동일, 인증·패키징·비용만 다름)
- A. 셀프호스트(사용자가 기기/서버 하나에 Docker 코어) — 무료 임베딩·데이터 본인, 운영자 비용 0
- B. 기기 하나를 서버로(데스크탑=코어, 나머지=클라, Tailscale) — 개인 멀티기기의 최저 마찰
- C. 운영형 SaaS(내가 서버·임베딩·저장 대행, 구독료 회수) — UX 최고, 임베딩 compute·저장·데이터책임을 내가 부담

## 9. 추후 도입 예정 설정 (config 표면 미리보기)

> 현재는 `config.env`(KEY=VALUE, 환경변수 우선) 한 파일로 CLI·스케줄러·웹 공통. 아래는 배포·멀티기기·확장 단계에서 **추가될 설정 항목의 예상 목록**(지금은 미구현, 방향만).

**벡터/저장 (8.1 관련)**
- `CHATMEM_VECTOR_BACKEND` = `npy`(기본, 로컬 빠름) / `sqlite-vec`(배포·저RAM)
- `CHATMEM_VECTOR_QUANT` = `float32` / `int8`(1/4 용량) / `binary`(재랭킹용)
- `CHATMEM_RERANK` = int8/binary 거친검색 후 fp32 원본 재랭킹 on/off
- (규모 시) `CHATMEM_VECTOR_BACKEND=pgvector` + 접속 URL

**임베딩 (저사양 대응)**
- `CHATMEM_EMBED_MODEL` 이미 있음 — 저RAM용 `e5-small(384d)` 선택지 문서화
- `CHATMEM_EMBED_DEVICE` = cpu/gpu(내장그래픽 포함) — 있으면 가속
- `CHATMEM_EMBED_REMOTE_URL` = 임베딩을 중앙 서버에 위임(클라이언트는 모델 없이)

**배포/네트워크 (8.2 관련)**
- `CHATMEM_MODE` = `local`(단일) / `client`(얇은 클라) / `server`(중앙 코어)
- `CHATMEM_SERVER_URL` = 클라이언트가 붙을 중앙 코어 주소
- `CHATMEM_AUTH_TOKEN` = 인그레스 인증 토큰(사용자별)
- `CHATMEM_DEVICE_NAME` = 이 기기 태그(멀티기기 필터용)
- `CHATMEM_BIND` = 서버 바인드 주소/포트(현재 웹 127.0.0.1:8642 고정)

**캡처 범위 (4.6 "제외목록 여지")**
- `CHATMEM_EXCLUDE_PROJECTS` / `CHATMEM_EXCLUDE_SESSIONS` = 특정 프로젝트·세션 색인 제외
- `CHATMEM_CAPTURE_SIDECHAIN` = 서브에이전트 대화 포함 여부(현재 제외)
- `CHATMEM_REDACT` = 저장 전 시크릿 패턴 마스킹 on/off(현재 원문 그대로)

**동작/스케줄 (이미 일부 존재)**
- 있음: `CHATMEM_ENRICH_BACKEND`(+모델별), `MIN_FREE_MB`, `IDLE_SECS`, `CHECKPOINT_TURNS`, `EMBED_BATCH`
- 추가 예상: `CHATMEM_ENRICH_TIME`(야간 정제 시각, 현재 04:00 고정), `CHATMEM_INDEX_INTERVAL`(현재 10분 고정), `CHATMEM_LOG_LEVEL`
